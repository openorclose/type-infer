{
  "name": "type-infer",
  "version": "0.0.1",
  "description": "A type inference tool for the Source 1 language.",
  "main": "index.js",
  "scripts": {
    "build": "./node_modules/.bin/tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Hill Tse",
  "license": "ISC",
  "dependencies": {
    "acorn": "^7.1.0"
  },
  "devDependencies": {
    "typescript": "^3.7.5"
  }
}
